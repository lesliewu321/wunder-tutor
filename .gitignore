node_modules
dist
.env
.env.*
!.env.example
*.log
.DS_Store
.vite
*.tsbuildinfo
server/.cache
.wrangler
.dev.vars
.astro
eval/.cache

# The translation's checking list (npm run i18n:export writes them; the page is published privately as an Artifact)
i18n-review.json
i18n-source.json
i18n-review.csv
i18n-review.html

# Volunteers' recordings (children's voices) — never commit
eval/volunteers
eval/contributions/

# Signing keys and release bundles never go into git (the upload key stays with its owner).
*.jks
*.keystore
*.aab

# Downloaded reference PDFs (VOA, FSI): tens of MB, public sources, not ours to version.
astra-lessons/resources/*.pdf
