<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        The microphone is the app: a learner speaks and hears what to fix. Android asks the family for it the first
        time the mic button is pressed, never at install.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
        Asked for together with the microphone, and it must be here. When the page calls for the microphone, Capacitor
        asks Android for RECORD_AUDIO and MODIFY_AUDIO_SETTINGS at once, and treats the answer as yes only if every
        one of them came back yes (BridgeWebChromeClient.onPermissionRequest). A permission a manifest never declared
        always comes back no — so leaving this line out would silently refuse the microphone to every learner.
        It is granted on install and shows no dialog: it lets the app set the audio route while recording.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- "Say it right": a photo of a page from a book, taken inside the app. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Neither is required to install: a phone without a camera can still do every lesson by typing. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
