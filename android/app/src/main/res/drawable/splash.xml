<?xml version="1.0" encoding="utf-8"?>
<!--
  What fills the screen between tapping the icon and the app's first screen appearing — well under a second, since
  the app's files are already on the device.

  It is the app's cream with Pip in the middle, so the icon, this, and the first screen are one continuous thing. Drawn
  rather than photographed (a vector, not a PNG per screen size), so it is sharp on any phone or tablet, in portrait
  or landscape, at no weight.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:drawable="@drawable/pip"
        android:gravity="center"
        android:width="220dp"
        android:height="220dp" />
</layer-list>
