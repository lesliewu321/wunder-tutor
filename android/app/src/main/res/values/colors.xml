<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The cream the app opens on (the bg token in src/styles/tokens.css). values-night/ has the dark one, so a phone
      set to dark does not get a bright flash before the app's own first screen arrives.
    -->
    <color name="splash_background">#FFF8EE</color>
</resources>
