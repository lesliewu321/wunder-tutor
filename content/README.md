# Wunder Tutor curriculum

English and Putonghua course data lives in `courses/`. The application reads it through
`src/content/load.ts`. Existing lesson and item identifiers are preserved when adding content.

Run `npm run content:check` to validate the data before publishing.
